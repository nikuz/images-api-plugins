{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "ImagesUploader.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/",
      "handler": "ImagesUploader.upload",
      "config": {
        "policies": []
      }
    }
  ]
}
